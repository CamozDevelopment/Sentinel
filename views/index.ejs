<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sentinel - Elite Discord Security & Moderation Bot</title>
    <meta name="description" content="Enterprise-grade Discord security with anti-nuke, auto-mod, anti-spam, and powerful moderation tools. Manage everything from a stunning web dashboard.">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link href="/css/style.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #5865f2;
            --secondary: #7289da;
            --accent: #eb459e;
            --success: #3ba55d;
            --warning: #faa61a;
            --danger: #ed4245;
            --dark: #0a0e27;
            --dark-alt: #0f1629;
            --surface: #1a1f3a;
            --surface-light: #252b48;
            --text: #ffffff;
            --text-secondary: #b9bbbe;
            --text-muted: #72767d;
            --border: rgba(88, 101, 242, 0.2);
            --glow: rgba(88, 101, 242, 0.4);
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: var(--dark);
            color: var(--text);
            overflow-x: hidden;
            line-height: 1.6;
            scroll-behavior: smooth;
            -webkit-font-smoothing: antialiased;
        }

        /* Animated Background */
        .animated-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            background: linear-gradient(180deg, var(--dark) 0%, var(--dark-alt) 50%, var(--surface) 100%);
            overflow: hidden;
        }

        .gradient-orb {
            position: absolute;
            border-radius: 50%;
            filter: blur(120px);
            opacity: 0.25;
            animation: float 25s infinite ease-in-out;
        }

        .orb-1 {
            width: 700px;
            height: 700px;
            background: radial-gradient(circle, var(--primary), transparent);
            top: -250px;
            left: -150px;
        }

        .orb-2 {
            width: 600px;
            height: 600px;
            background: radial-gradient(circle, var(--accent), transparent);
            bottom: -200px;
            right: -150px;
            animation-delay: 8s;
        }

        .orb-3 {
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, var(--secondary), transparent);
            top: 50%;
            right: 10%;
            animation-delay: 15s;
        }

        @keyframes float {
            0%, 100% { transform: translate(0, 0) scale(1); }
            33% { transform: translate(100px, -120px) scale(1.15); }
            66% { transform: translate(-80px, 100px) scale(0.9); }
        }

        /* Navbar */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 1.25rem 6%;
            background: rgba(10, 14, 39, 0.8);
            backdrop-filter: blur(24px) saturate(180%);
            border-bottom: 1px solid var(--border);
            z-index: 1000;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        nav.scrolled {
            padding: 0.875rem 6%;
            background: rgba(10, 14, 39, 0.95);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.5);
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 0.875rem;
            font-size: 1.5rem;
            font-weight: 800;
            color: var(--text);
            text-decoration: none;
            transition: transform 0.3s;
            letter-spacing: -0.5px;
        }

        .logo:hover {
            transform: scale(1.03);
        }

        .logo-icon {
            width: 46px;
            height: 46px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            box-shadow: 0 8px 24px var(--glow);
            transition: all 0.3s;
        }

        .logo:hover .logo-icon {
            box-shadow: 0 12px 32px var(--glow);
            transform: rotate(-5deg);
        }

        .nav-links {
            display: flex;
            gap: 2.5rem;
            align-items: center;
        }

        .nav-link {
            color: var(--text-secondary);
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s;
            font-size: 0.95rem;
            position: relative;
            letter-spacing: -0.2px;
        }

        .nav-link::after {
            content: '';
            position: absolute;
            bottom: -6px;
            left: 0;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, var(--primary), var(--secondary));
            transition: width 0.3s;
        }

        .nav-link:hover {
            color: var(--text);
        }

        .nav-link:hover::after {
            width: 100%;
        }

        .btn {
            padding: 0.875rem 2rem;
            border-radius: 12px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            display: inline-flex;
            align-items: center;
            gap: 0.625rem;
            font-size: 0.95rem;
            border: none;
            cursor: pointer;
            letter-spacing: -0.2px;
            position: relative;
            overflow: hidden;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        .btn:hover::before {
            width: 300px;
            height: 300px;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: var(--text);
            box-shadow: 0 8px 24px var(--glow);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 32px rgba(88, 101, 242, 0.6);
        }

        .btn-secondary {
            background: rgba(88, 101, 242, 0.12);
            color: var(--text);
            border: 2px solid var(--border);
            backdrop-filter: blur(10px);
        }

        .btn-secondary:hover {
            background: rgba(88, 101, 242, 0.2);
            border-color: var(--primary);
            transform: translateY(-2px);
        }

        .btn-outline {
            background: transparent;
            color: var(--text);
            border: 2px solid rgba(255, 255, 255, 0.3);
        }

        .btn-outline:hover {
            background: rgba(255, 255, 255, 0.08);
            border-color: var(--text);
        }

        /* Hero Section */
        .hero {
            position: relative;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 0 5%;
            overflow: hidden;
        }

        .hero-content {
            max-width: 1100px;
            width: 100%;
            z-index: 1;
            padding-top: 80px;
            text-align: center !important;
            margin: 0 auto;
        }

        .hero-content * {
            text-align: center;
        }

        .hero-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.625rem;
            padding: 0.75rem 1.75rem;
            background: rgba(88, 101, 242, 0.12);
            border: 1px solid var(--border);
            border-radius: 100px;
            color: var(--primary);
            font-size: 0.875rem;
            font-weight: 700;
            margin-bottom: 2.5rem;
            animation: fadeSlideDown 0.8s cubic-bezier(0.4, 0, 0.2, 1);
            backdrop-filter: blur(10px);
            letter-spacing: 0.5px;
            text-transform: uppercase;
        }

        .hero-badge i {
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% {
                opacity: 1;
                transform: scale(1);
            }
            50% {
                opacity: 0.6;
                transform: scale(1.15);
            }
        }

        .hero h1 {
            font-size: 5.5rem;
            font-weight: 900;
            line-height: 1.08;
            margin-bottom: 2rem;
            background: linear-gradient(135deg, #ffffff 0%, var(--primary) 40%, var(--accent) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: fadeSlideUp 0.8s cubic-bezier(0.4, 0, 0.2, 1) 0.2s both;
            letter-spacing: -2px;
        }

        .hero-description {
            font-size: 1.4rem;
            color: var(--text-secondary);
            line-height: 1.75;
            animation: fadeSlideUp 0.8s cubic-bezier(0.4, 0, 0.2, 1) 0.4s both;
            max-width: 880px;
            margin: 0 auto 3.5rem auto;
            letter-spacing: -0.3px;
        }

        .hero-buttons {
            display: flex;
            gap: 1.5rem;
            justify-content: center;
            flex-wrap: wrap;
            animation: fadeSlideUp 0.8s cubic-bezier(0.4, 0, 0.2, 1) 0.6s both;
            margin-bottom: 5rem;
        }

        .btn-large {
            padding: 1.35rem 3.5rem;
            font-size: 1.125rem;
            border-radius: 14px;
            font-weight: 700;
        }

        .hero-stats {
            display: flex;
            gap: 5rem;
            justify-content: center;
            flex-wrap: wrap;
            animation: fadeSlideUp 0.8s cubic-bezier(0.4, 0, 0.2, 1) 0.8s both;
        }

        .stat-item {
            text-align: center;
        }

        .stat-number {
            font-size: 3.5rem;
            font-weight: 900;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            line-height: 1;
            margin-bottom: 0.75rem;
            letter-spacing: -1.5px;
        }

        .stat-label {
            color: var(--text-secondary);
            font-size: 0.95rem;
            font-weight: 600;
            letter-spacing: 0.5px;
            text-transform: uppercase;
        }

        /* Features Grid */
        .features {
            position: relative;
            padding: 8rem 6% 6rem;
            z-index: 1;
        }

        .section-header {
            text-align: center;
            margin-bottom: 5rem;
            animation: fadeSlideUp 0.8s ease;
        }

        .section-badge {
            display: inline-block;
            padding: 0.625rem 1.5rem;
            background: rgba(88, 101, 242, 0.12);
            border: 1px solid var(--border);
            border-radius: 100px;
            color: var(--primary);
            font-size: 0.8rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            letter-spacing: 1px;
            text-transform: uppercase;
        }

        .section-title {
            font-size: 3.5rem;
            font-weight: 900;
            margin-bottom: 1.5rem;
            background: linear-gradient(135deg, var(--text), var(--primary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -1px;
        }

        .section-subtitle {
            font-size: 1.3rem;
            color: var(--text-secondary);
            max-width: 750px;
            margin: 0 auto;
            line-height: 1.75;
            letter-spacing: -0.2px;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(380px, 1fr));
            gap: 2.5rem;
            max-width: 1500px;
            margin: 0 auto;
        }

        .feature-card {
            background: rgba(26, 31, 58, 0.6);
            border: 1px solid var(--border);
            border-radius: 24px;
            padding: 3rem;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
            backdrop-filter: blur(20px);
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--primary), var(--secondary), var(--accent));
            opacity: 0;
            transition: opacity 0.4s;
        }

        .feature-card::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at top right, rgba(88, 101, 242, 0.12), transparent);
            opacity: 0;
            transition: opacity 0.4s;
        }

        .feature-card:hover {
            transform: translateY(-12px) scale(1.02);
            border-color: var(--primary);
            box-shadow: 0 24px 72px rgba(88, 101, 242, 0.35);
        }

        .feature-card:hover::before,
        .feature-card:hover::after {
            opacity: 1;
        }

        .feature-icon {
            width: 85px;
            height: 85px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.4rem;
            margin-bottom: 2rem;
            box-shadow: 0 12px 36px var(--glow);
            position: relative;
            z-index: 1;
            transition: all 0.4s;
        }

        .feature-card:hover .feature-icon {
            transform: scale(1.12) rotate(-5deg);
            box-shadow: 0 16px 48px rgba(88, 101, 242, 0.6);
        }

        .feature-card h3 {
            font-size: 1.65rem;
            margin-bottom: 1.25rem;
            font-weight: 800;
            position: relative;
            z-index: 1;
            letter-spacing: -0.5px;
        }

        .feature-card p {
            color: var(--text-secondary);
            line-height: 1.8;
            font-size: 1.05rem;
            position: relative;
            z-index: 1;
            letter-spacing: -0.2px;
        }

        .feature-list {
            list-style: none;
            margin-top: 1.5rem;
            position: relative;
            z-index: 1;
        }

        .feature-list li {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.5rem 0;
            color: var(--text-secondary);
            font-size: 0.95rem;
        }

        .feature-list i {
            color: var(--success);
            font-size: 1rem;
        }

        /* Showcase Section */
        .showcase {
            position: relative;
            padding: 8rem 6%;
            z-index: 1;
            background: rgba(26, 31, 58, 0.4);
        }

        .showcase-container {
            max-width: 1500px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1.1fr 1fr;
            gap: 6rem;
            align-items: center;
        }

        .showcase-content h2 {
            font-size: 3.5rem;
            font-weight: 900;
            margin-bottom: 2rem;
            line-height: 1.15;
            background: linear-gradient(135deg, var(--text), var(--primary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -1px;
        }

        .showcase-content p {
            font-size: 1.2rem;
            color: var(--text-secondary);
            margin-bottom: 2.5rem;
            line-height: 1.85;
            letter-spacing: -0.2px;
        }

        .showcase-list {
            list-style: none;
            margin-bottom: 3rem;
        }

        .showcase-list li {
            display: flex;
            align-items: flex-start;
            gap: 1.25rem;
            padding: 1rem 0;
            font-size: 1.1rem;
            color: var(--text);
            transition: all 0.3s;
        }

        .showcase-list li:hover {
            transform: translateX(8px);
        }

        .showcase-list i {
            color: var(--primary);
            font-size: 1.4rem;
            min-width: 30px;
            margin-top: 2px;
        }

        .showcase-image {
            background: linear-gradient(135deg, rgba(88, 101, 242, 0.15), rgba(114, 137, 218, 0.1));
            border: 1px solid var(--border);
            border-radius: 28px;
            padding: 4rem;
            aspect-ratio: 16/11;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
            backdrop-filter: blur(20px);
            box-shadow: 0 24px 72px rgba(88, 101, 242, 0.25);
        }

        .showcase-image::before {
            content: '';
            position: absolute;
            inset: -50%;
            background: linear-gradient(45deg, transparent, rgba(88, 101, 242, 0.15), transparent);
            animation: shimmer 4s infinite;
        }

        @keyframes shimmer {
            0% {
                transform: translateX(-100%) translateY(-100%) rotate(45deg);
            }
            100% {
                transform: translateX(100%) translateY(100%) rotate(45deg);
            }
        }

        .showcase-image i {
            font-size: 12rem;
            background: linear-gradient(135deg, rgba(88, 101, 242, 0.6), rgba(114, 137, 218, 0.4));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            z-index: 1;
        }

        /* Commands Section */
        .commands {
            position: relative;
            padding: 8rem 6%;
            z-index: 1;
        }

        .commands-grid {
            max-width: 1500px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }

        .command-card {
            background: rgba(26, 31, 58, 0.5);
            border: 1px solid var(--border);
            border-radius: 20px;
            padding: 2.5rem;
            transition: all 0.3s;
        }

        .command-card:hover {
            transform: translateY(-8px);
            border-color: var(--primary);
            box-shadow: 0 16px 48px rgba(88, 101, 242, 0.25);
        }

        .command-header {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-bottom: 1.5rem;
        }

        .command-icon {
            width: 50px;
            height: 50px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.4rem;
            box-shadow: 0 8px 24px var(--glow);
        }

        .command-card h4 {
            font-size: 1.25rem;
            font-weight: 700;
        }

        .command-card p {
            color: var(--text-secondary);
            line-height: 1.7;
            margin-bottom: 1.5rem;
        }

        .command-code {
            background: rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            padding: 0.75rem 1rem;
            font-family: 'Courier New', monospace;
            font-size: 0.9rem;
            color: var(--primary);
        }

        /* CTA Section */
        .cta {
            position: relative;
            padding: 8rem 6%;
            z-index: 1;
            text-align: center;
        }

        .cta-box {
            max-width: 1100px;
            margin: 0 auto;
            background: linear-gradient(135deg, var(--primary), var(--secondary), var(--accent));
            border-radius: 32px;
            padding: 6rem 4rem;
            box-shadow: 0 32px 96px rgba(88, 101, 242, 0.5);
            position: relative;
            overflow: hidden;
        }

        .cta-box::before {
            content: '';
            position: absolute;
            inset: 0;
            background: radial-gradient(circle at top right, rgba(255, 255, 255, 0.15), transparent 70%);
        }

        .cta-box::after {
            content: '';
            position: absolute;
            inset: -50%;
            background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.05), transparent);
            animation: shimmer 5s infinite;
        }

        .cta-box h2 {
            font-size: 3.5rem;
            font-weight: 900;
            margin-bottom: 1.5rem;
            position: relative;
            z-index: 1;
            letter-spacing: -1px;
        }

        .cta-box p {
            font-size: 1.4rem;
            margin-bottom: 3rem;
            opacity: 0.95;
            position: relative;
            z-index: 1;
            letter-spacing: -0.2px;
        }

        .cta-box .hero-buttons {
            position: relative;
            z-index: 1;
            margin-bottom: 0;
        }

        /* Footer */
        footer {
            position: relative;
            padding: 4rem 6%;
            background: rgba(10, 14, 39, 0.95);
            border-top: 1px solid var(--border);
            text-align: center;
            z-index: 1;
        }

        .footer-content {
            max-width: 1400px;
            margin: 0 auto;
        }

        .footer-links {
            display: flex;
            justify-content: center;
            gap: 3rem;
            margin-bottom: 2rem;
            flex-wrap: wrap;
        }

        .footer-link {
            color: var(--text-secondary);
            text-decoration: none;
            transition: all 0.3s;
            font-weight: 600;
            font-size: 0.95rem;
        }

        .footer-link:hover {
            color: var(--primary);
            transform: translateY(-2px);
        }

        .footer-text {
            color: var(--text-muted);
            font-size: 0.95rem;
            margin-bottom: 1rem;
        }

        .footer-socials {
            display: flex;
            gap: 1.5rem;
            justify-content: center;
            margin-top: 2rem;
        }

        .social-link {
            width: 45px;
            height: 45px;
            background: rgba(88, 101, 242, 0.12);
            border: 1px solid var(--border);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--text-secondary);
            text-decoration: none;
            transition: all 0.3s;
            font-size: 1.2rem;
        }

        .social-link:hover {
            background: var(--primary);
            color: var(--text);
            transform: translateY(-3px);
            box-shadow: 0 8px 24px var(--glow);
        }

        /* Animations */
        @keyframes fadeSlideUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeSlideDown {
            from {
                opacity: 0;
                transform: translateY(-50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Responsive */
        @media (max-width: 1200px) {
            .showcase-container {
                grid-template-columns: 1fr;
                gap: 4rem;
            }

            .showcase-image {
                order: -1;
            }

            .features-grid {
                grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            }
        }

        @media (max-width: 768px) {
            nav {
                padding: 1rem 5%;
            }

            .nav-links {
                gap: 1rem;
            }

            .nav-link:not(.btn) {
                display: none;
            }

            .hero h1 {
                font-size: 3.5rem;
            }

            .hero p.hero-description {
                font-size: 1.2rem;
            }

            .hero-stats {
                gap: 3rem;
            }

            .stat-number {
                font-size: 2.5rem;
            }

            .section-title {
                font-size: 2.5rem;
            }

            .showcase-content h2 {
                font-size: 2.5rem;
            }

            .features-grid {
                grid-template-columns: 1fr;
            }

            .commands-grid {
                grid-template-columns: 1fr;
            }

            .cta-box {
                padding: 4rem 2.5rem;
            }

            .cta-box h2 {
                font-size: 2.5rem;
            }

            .btn-large {
                padding: 1.1rem 2.5rem;
                font-size: 1rem;
            }
        }

        @media (max-width: 480px) {
            .hero h1 {
                font-size: 2.5rem;
            }

            .section-title {
                font-size: 2rem;
            }

            .showcase-content h2 {
                font-size: 2rem;
            }

            .cta-box h2 {
                font-size: 2rem;
            }
        }

        /* Scroll to top button */
        .scroll-top {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            width: 50px;
            height: 50px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--text);
            font-size: 1.2rem;
            cursor: pointer;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s;
            box-shadow: 0 8px 24px var(--glow);
            z-index: 999;
        }

        .scroll-top.visible {
            opacity: 1;
            visibility: visible;
        }

        .scroll-top:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 32px rgba(88, 101, 242, 0.6);
        }
    </style>
</head>
<body>
    <!-- Animated Background -->
    <div class="animated-bg">
        <div class="gradient-orb orb-1"></div>
        <div class="gradient-orb orb-2"></div>
        <div class="gradient-orb orb-3"></div>
    </div>

    <!-- Navbar -->
    <nav id="navbar">
        <a href="/" class="logo">
            <div class="logo-icon">
                <i class="fas fa-shield-halved"></i>
            </div>
            <span>Sentinel</span>
        </a>
        <div class="nav-links">
            <a href="#features" class="nav-link">Features</a>
            <a href="/commands" class="nav-link">Commands</a>
            <a href="/docs" class="nav-link">Docs</a>
            <% if (user) { %>
                <a href="/dashboard" class="btn btn-primary">
                    <i class="fas fa-gauge-high"></i>
                    Dashboard
                </a>
            <% } else { %>
                <a href="/login" class="btn btn-primary">
                    <i class="fab fa-discord"></i>
                    Login
                </a>
            <% } %>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <div class="hero-badge">
                <i class="fas fa-shield-halved"></i>
                <span>Enterprise Discord Security</span>
            </div>
            <h1>Protect Your Discord Community with Sentinel</h1>
            <p class="hero-description">
                Enterprise-grade security & moderation powered by advanced AI. Features anti-nuke protection, 
                intelligent auto-moderation, spam detection, and a stunning web dashboard. Everything you need to keep your server safe.
            </p>
            <div class="hero-buttons">
                <% if (user) { %>
                    <a href="/dashboard" class="btn btn-primary btn-large">
                        <i class="fas fa-gauge-high"></i>
                        Open Dashboard
                    </a>
                <% } else { %>
                    <a href="/login" class="btn btn-primary btn-large">
                        <i class="fab fa-discord"></i>
                        Get Started Free
                    </a>
                <% } %>
                <a href="https://discord.com/api/oauth2/authorize?client_id=1457723088627175435&permissions=8&scope=bot%20applications.commands" target="_blank" class="btn btn-outline btn-large">
                    <i class="fas fa-plus"></i>
                    Add to Server
                </a>
            </div>
            <div class="hero-stats">
                <div class="stat-item">
                    <div class="stat-number">99.9%</div>
                    <div class="stat-label">Uptime</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number">&lt;50ms</div>
                    <div class="stat-label">Response Time</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number">24/7</div>
                    <div class="stat-label">Protection</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features" id="features">
        <div class="section-header">
            <div class="section-badge">Powerful Features</div>
            <h2 class="section-title">Everything You Need to Secure Your Server</h2>
            <p class="section-subtitle">
                Comprehensive moderation tools, advanced security systems, and beautiful management dashboard - all in one powerful bot
            </p>
        </div>
        <div class="features-grid">
            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-robot"></i>
                </div>
                <h3>Smart Auto-Moderation</h3>
                <p>
                    Intelligent content filtering with machine learning-powered detection for spam, toxicity, and inappropriate content.
                </p>
                <ul class="feature-list">
                    <li><i class="fas fa-check-circle"></i> Wildcard pattern matching</li>
                    <li><i class="fas fa-check-circle"></i> Zalgo text detection</li>
                    <li><i class="fas fa-check-circle"></i> Emoji spam prevention</li>
                    <li><i class="fas fa-check-circle"></i> Custom blocked words</li>
                    <li><i class="fas fa-check-circle"></i> Mass mention protection</li>
                </ul>
            </div>

            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-bomb"></i>
                </div>
                <h3>Anti-Nuke Protection</h3>
                <p>
                    Real-time detection and prevention of malicious server attacks with instant response and rollback capabilities.
                </p>
                <ul class="feature-list">
                    <li><i class="fas fa-check-circle"></i> Channel deletion monitoring</li>
                    <li><i class="fas fa-check-circle"></i> Role mass-deletion alerts</li>
                    <li><i class="fas fa-check-circle"></i> Excessive ban detection</li>
                    <li><i class="fas fa-check-circle"></i> Instant punishment</li>
                    <li><i class="fas fa-check-circle"></i> Customizable thresholds</li>
                </ul>
            </div>

            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-shield-virus"></i>
                </div>
                <h3>Anti-Spam System</h3>
                <p>
                    Advanced spam detection monitoring duplicate messages, images, and rapid posting with automatic enforcement.
                </p>
                <ul class="feature-list">
                    <li><i class="fas fa-check-circle"></i> Duplicate message detection</li>
                    <li><i class="fas fa-check-circle"></i> Image spam protection</li>
                    <li><i class="fas fa-check-circle"></i> Rate limit enforcement</li>
                    <li><i class="fas fa-check-circle"></i> Smart timeouts</li>
                    <li><i class="fas fa-check-circle"></i> Whitelist system</li>
                </ul>
            </div>

            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-globe"></i>
                </div>
                <h3>Global Ban Network</h3>
                <p>
                    Cross-server ban synchronization to protect your community from known troublemakers across Discord.
                </p>
                <ul class="feature-list">
                    <li><i class="fas fa-check-circle"></i> Cross-server bans</li>
                    <li><i class="fas fa-check-circle"></i> Appeal system</li>
                    <li><i class="fas fa-check-circle"></i> Auto server invites</li>
                    <li><i class="fas fa-check-circle"></i> Ban evidence tracking</li>
                    <li><i class="fas fa-check-circle"></i> Flexible punishments</li>
                </ul>
            </div>

            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-scroll"></i>
                </div>
                <h3>Comprehensive Logging</h3>
                <p>
                    Full audit trail of all server activity with detailed logs for messages, members, roles, and moderation actions.
                </p>
                <ul class="feature-list">
                    <li><i class="fas fa-check-circle"></i> Message edit/delete logs</li>
                    <li><i class="fas fa-check-circle"></i> Member join/leave tracking</li>
                    <li><i class="fas fa-check-circle"></i> Role change monitoring</li>
                    <li><i class="fas fa-check-circle"></i> Moderation action logs</li>
                    <li><i class="fas fa-check-circle"></i> Channel activity tracking</li>
                </ul>
            </div>

            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-paint-brush"></i>
                </div>
                <h3>Web Dashboard</h3>
                <p>
                    Beautiful, modern web interface with Discord OAuth2, real-time updates, and intuitive configuration management.
                </p>
                <ul class="feature-list">
                    <li><i class="fas fa-check-circle"></i> Discord OAuth2 login</li>
                    <li><i class="fas fa-check-circle"></i> Real-time configuration</li>
                    <li><i class="fas fa-check-circle"></i> Bulk editing tools</li>
                    <li><i class="fas fa-check-circle"></i> Mobile responsive</li>
                    <li><i class="fas fa-check-circle"></i> Dark/Light themes</li>
                </ul>
            </div>
        </div>
    </section>

    <!-- Dashboard Showcase -->
    <section class="showcase" id="dashboard">
        <div class="showcase-container">
            <div class="showcase-content">
                <h2>Stunning Web Dashboard</h2>
                <p>
                    Manage every aspect of your server security from one beautiful, intuitive interface. 
                    Configure settings, review logs, and moderate your community without typing a single command.
                </p>
                <ul class="showcase-list">
                    <li>
                        <i class="fas fa-bolt"></i>
                        <span><strong>Real-time Updates:</strong> See configuration changes instantly without bot restarts</span>
                    </li>
                    <li>
                        <i class="fas fa-palette"></i>
                        <span><strong>Modern UI:</strong> Glass morphism design with smooth animations and transitions</span>
                    </li>
                    <li>
                        <i class="fas fa-droplet"></i>
                        <span><strong>Easy Dropdowns:</strong> Select roles and channels visually - no more copying IDs</span>
                    </li>
                    <li>
                        <i class="fas fa-layer-group"></i>
                        <span><strong>Bulk Management:</strong> Add/remove multiple blocked words and settings at once</span>
                    </li>
                    <li>
                        <i class="fas fa-mobile-screen-button"></i>
                        <span><strong>Mobile Friendly:</strong> Fully responsive design works perfectly on any device</span>
                    </li>
                    <li>
                        <i class="fas fa-keyboard"></i>
                        <span><strong>Keyboard Shortcuts:</strong> Power user features like Ctrl+S to save instantly</span>
                    </li>
                </ul>
                <% if (!user) { %>
                    <a href="/login" class="btn btn-primary btn-large">
                        <i class="fas fa-gauge-high"></i>
                        Access Dashboard
                    </a>
                <% } %>
            </div>
            <div class="showcase-image">
                <i class="fas fa-desktop"></i>
            </div>
        </div>
    </section>

    <!-- Commands Section -->
    <section class="commands" id="commands">
        <div class="section-header">
            <div class="section-badge">Command Library</div>
            <h2 class="section-title">Powerful Moderation Commands</h2>
            <p class="section-subtitle">
                Comprehensive suite of slash commands for complete server management and moderation control
            </p>
        </div>
        <div class="commands-grid">
            <div class="command-card">
                <div class="command-header">
                    <div class="command-icon">
                        <i class="fas fa-hammer"></i>
                    </div>
                    <h4>Basic Moderation</h4>
                </div>
                <p>Essential moderation actions for day-to-day server management</p>
                <div class="command-code">/ban • /kick • /timeout • /warn</div>
            </div>

            <div class="command-card">
                <div class="command-header">
                    <div class="command-icon">
                        <i class="fas fa-broom"></i>
                    </div>
                    <h4>Message Management</h4>
                </div>
                <p>Bulk message deletion and channel cleanup tools</p>
                <div class="command-code">/purge • /slowmode • /clearwarns</div>
            </div>

            <div class="command-card">
                <div class="command-header">
                    <div class="command-icon">
                        <i class="fas fa-lock"></i>
                    </div>
                    <h4>Channel Control</h4>
                </div>
                <p>Lock and unlock channels during emergencies or events</p>
                <div class="command-code">/lock • /unlock • /lockdown</div>
            </div>

            <div class="command-card">
                <div class="command-header">
                    <div class="command-icon">
                        <i class="fas fa-cog"></i>
                    </div>
                    <h4>Server Settings</h4>
                </div>
                <p>Configure security, logging, and moderation settings</p>
                <div class="command-code">/settings • /serversettings</div>
            </div>

            <div class="command-card">
                <div class="command-header">
                    <div class="command-icon">
                        <i class="fas fa-globe"></i>
                    </div>
                    <h4>Global Bans</h4>
                </div>
                <p>Manage cross-server bans and review appeals</p>
                <div class="command-code">/globalban • /reviewappeal</div>
            </div>

            <div class="command-card">
                <div class="command-header">
                    <div class="command-icon">
                        <i class="fas fa-info-circle"></i>
                    </div>
                    <h4>Information</h4>
                </div>
                <p>View server, user, and bot information</p>
                <div class="command-code">/userinfo • /serverinfo • /botinfo</div>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="cta">
        <div class="cta-box">
            <h2>Ready to Secure Your Discord Server?</h2>
            <p>Join thousands of communities protected by Sentinel's enterprise security</p>
            <div class="hero-buttons">
                <a href="https://discord.com/api/oauth2/authorize?client_id=1457723088627175435&permissions=8&scope=bot%20applications.commands" target="_blank" class="btn btn-outline btn-large">
                    <i class="fas fa-plus"></i>
                    Add to Discord
                </a>
                <% if (!user) { %>
                    <a href="/login" class="btn btn-outline btn-large">
                        <i class="fab fa-discord"></i>
                        Login with Discord
                    </a>
                <% } %>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="footer-content">
            <div class="footer-links">
                <a href="#features" class="footer-link">Features</a>
                <a href="#dashboard" class="footer-link">Dashboard</a>
                <a href="#commands" class="footer-link">Commands</a>
                <a href="#" class="footer-link">Documentation</a>
                <a href="#" class="footer-link">Support</a>
                <a href="#" class="footer-link">Privacy</a>
                <a href="#" class="footer-link">Terms</a>
            </div>
            <div class="footer-socials">
                <a href="#" class="social-link" title="Discord">
                    <i class="fab fa-discord"></i>
                </a>
                <a href="#" class="social-link" title="GitHub">
                    <i class="fab fa-github"></i>
                </a>
                <a href="#" class="social-link" title="Twitter">
                    <i class="fab fa-twitter"></i>
                </a>
            </div>
            <p class="footer-text">
                © 2026 Sentinel Bot. All rights reserved. Made with <i class="fas fa-heart" style="color: var(--accent);"></i> for Discord communities.
            </p>
        </div>
    </footer>

    <!-- Scroll to Top Button -->
    <div class="scroll-top" id="scrollTop">
        <i class="fas fa-arrow-up"></i>
    </div>

    <script>
        // Navbar scroll effect
        const navbar = document.getElementById('navbar');
        const scrollTop = document.getElementById('scrollTop');

        window.addEventListener('scroll', () => {
            if (window.scrollY > 100) {
                navbar.classList.add('scrolled');
                scrollTop.classList.add('visible');
            } else {
                navbar.classList.remove('scrolled');
                scrollTop.classList.remove('visible');
            }
        });

        // Smooth scroll for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                const href = this.getAttribute('href');
                if (href !== '#') {
                    e.preventDefault();
                    const target = document.querySelector(href);
                    if (target) {
                        target.scrollIntoView({
                            behavior: 'smooth',
                            block: 'start'
                        });
                    }
                }
            });
        });

        // Scroll to top button
        scrollTop.addEventListener('click', () => {
            window.scrollTo({
                top: 0,
                behavior: 'smooth'
            });
        });

        // Add animation on scroll
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        document.querySelectorAll('.feature-card, .command-card, .showcase-container').forEach(el => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(30px)';
            el.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
            observer.observe(el);
        });
    </script>
</body>
</html>
