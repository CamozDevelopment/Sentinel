<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= error %> - Sentinel</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#5865f2',
                        secondary: '#7289da',
                        accent: '#eb459e',
                        dark: '#0a0e27',
                        'dark-alt': '#0f1629',
                        surface: '#1a1f3a'
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-gradient-to-br from-dark via-dark-alt to-surface text-white min-h-screen flex items-center justify-center px-6">
    <!-- Animated Background -->
    <div class="fixed inset-0 overflow-hidden pointer-events-none z-0">
        <div class="absolute top-0 -left-40 w-96 h-96 bg-red-600 rounded-full mix-blend-multiply filter blur-3xl opacity-20"></div>
        <div class="absolute bottom-0 -right-40 w-96 h-96 bg-red-800 rounded-full mix-blend-multiply filter blur-3xl opacity-20"></div>
    </div>

    <div class="relative z-10 text-center max-w-2xl">
        <div class="inline-block p-8 bg-red-600/20 border-4 border-red-500/40 rounded-full mb-8">
            <i class="fas fa-ban text-8xl text-red-500"></i>
        </div>
        
        <h1 class="text-6xl font-black mb-6 bg-gradient-to-r from-red-400 to-red-600 bg-clip-text text-transparent">
            <%= error %>
        </h1>
        
        <p class="text-2xl text-gray-300 mb-12 leading-relaxed">
            <%= message %>
        </p>

        <div class="flex flex-wrap items-center justify-center gap-4">
            <a href="/" class="px-8 py-4 bg-gradient-to-r from-primary to-secondary rounded-xl font-bold text-lg hover:shadow-2xl hover:shadow-primary/50 transition-all hover:scale-105">
                <i class="fas fa-home mr-2"></i>Go Home
            </a>
            <% if (user) { %>
                <a href="/dashboard" class="px-8 py-4 bg-white/5 border-2 border-white/20 rounded-xl font-bold text-lg hover:bg-white/10 hover:border-white/40 transition-all hover:scale-105">
                    <i class="fas fa-gauge-high mr-2"></i>Dashboard
                </a>
            <% } else { %>
                <a href="/login" class="px-8 py-4 bg-white/5 border-2 border-white/20 rounded-xl font-bold text-lg hover:bg-white/10 hover:border-white/40 transition-all hover:scale-105">
                    <i class="fab fa-discord mr-2"></i>Login
                </a>
            <% } %>
        </div>
    </div>
</body>
</html>
